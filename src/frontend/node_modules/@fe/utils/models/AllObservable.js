/**
 * @file   AllObservable
 * @author yucong02
 */
import {
  extendObservable,
  toJS
} from 'mobx'
import Root from './Root'

export default class AllObservable extends Root {

  assignShallow(init) {
    // super.assignShallow(init)
    extendObservable(this, toJS(init))
  }

}